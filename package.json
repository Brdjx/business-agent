{
  "name": "business-agent",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "description": "An agentic assistant over a small consulting business's records: reads clients, projects, invoices and time entries, proposes writes for per-action approval, and is measured by a mechanical eval suite.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Brdjx/business-agent.git"
  },
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d --wait",
    "db:check": "docker compose exec -T db psql -v ON_ERROR_STOP=1 -U business_agent -d business_agent -f /dev/stdin < scripts/assert-roles.sql",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ask": "tsx --env-file-if-exists=.env src/cli.ts",
    "eval": "tsx --env-file-if-exists=.env src/agent/evals/run.ts",
    "eval:history": "tsx --env-file-if-exists=.env src/agent/evals/history.ts",
    "//env": "--env-file-if-exists rather than --env-file: the flag makes `cp .env.example .env` followed by `npm run ask` work, which is what the README tells a reader to do and what it did not do. The -if-exists form is used so the scripts still run for anyone who exports the variables instead, rather than failing on a file they never wanted."
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "//optionalDependencies": "Only the bedrock provider imports this, and it does so with a dynamic import, so the default anthropic path never loads it. Optional rather than a plain dependency because an install that pulls an AWS SDK to talk to api.anthropic.com invites the reasonable question of what else is being sent where.",
  "optionalDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.990.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
